# 实施验收记录

日期：2026-09-25。运行环境：当前 Mac、Node.js 24、Codex CLI 0.145.0、用户当前的 ChatGPT Pro 登录。

## 已通过

- TypeScript 类型检查和 Vite 生产构建。
- 19 项自动化测试：XLSX 缺列、空值、重复与重复提交；全局并发 3；审核释放名额；提示词更新和快照恢复；图片／文案中断；结果落盘后数据库提交前中断；清理中断及清理失败恢复；重复审核与发布；限流三次停止；额度／登录／代理阻塞；图片数量、路径、解码和重复校验；执行超时与孤儿进程终止；本地 HTTP 接口、SSE、原图下载与 ZIP 导出。
- 浏览器检查生成工作台、运行设置、素材库和发布入口。
- 已运行服务的重复启动：打开同一服务，不另起任务队列。
- 正式工具的一次真实图片阶段中断与重启：原进程结束、旧执行清理、按原提示词快照恢复，产生新的执行编号。
- 六张图片进入待审核后再次正常停止／启动：审核状态与全部原图保留；浏览器放大与翻页正常。

## 真实模型调用记录

- 正式工作台新建 momo／工业机器人系统集成产品选型方案PPT，用订阅 CLI 顺序生成六张图片。旧验证素材未导入正式数据库。
- 用户已明确通过这组六张图片。随后文案阶段传入实际原图，生成标题“工业机器人系统集成选型PPT”（14 字符）、正文 590 字符，已通过格式校验并进入文案待审核。
- 并发小测：在一个正式生图阶段运行期间，另起两条隔离的看图写文案任务，合计三个 CLI 阶段。两条文案均结束：一条通过校验（标题“工业机器人选型PPT分享”，正文 583 字符），一条因结果格式校验未通过进入异常；没有自动入库或无限重试。隔离结果在 `validation/app-smoke`，不属于正式素材库。
- 图片阶段使用生成工具并写入清单，最终校验原图；文案阶段使用实际原图参数和 JSON Schema。
- 本次不能推断 Plus 的实际安全并发上限，也不能保证今后 Pro 不限流。

## 人工验收节点

用户已依次明确通过图片与文案，两次审核均由工具接口推进，真实素材已保存为“未发布”。“生图 → 人工通过 → 看图写文案 → 人工通过 → 完整素材入库”的真实链路已通过。正式素材没有标记发布；发布状态修改及重复提交由隔离自动化测试验证。

## 素材删除与图片张数调整

- 素材库「未发布」页卡片左下角添加删除图标及整组永久删除确认；浏览器检查图标位置和取消行为正常。
- 删除接口清理任务、执行、素材文件关联、清理状态和相关导入预览缓存；图片及文案执行目录一并删除，保留其他素材。重复请求、陈旧版本、已发布／未完成任务受到状态校验。
- 删除中断后可恢复清理，不重新派发生成。
- 移除固定图片张数配置、CLI 指令和图片 Schema 的固定张数限制；非空图片清单可为不同张数，文案附带实际全部原图。
- 更新后构建通过、23 项测试全部通过，包括新增整组删除、删除恢复、不同图片张数与 CLI 参数测试。本次使用隔离测试数据验证实际删除，正式素材保留供用户使用入口自行处理。

## 文案长度改为提示词控制

- 移除文案 JSON Schema 的 minLength/maxLength、应用层字符数量校验，以及 CLI 附加指令中的固定字数要求。
- 保留 title/body 必需字符串结构与应用层非空检查，界面字符数仍仅作为统计展示。
- 短文案、长标题／长正文均通过回归测试；缺失、类型错误和纯空白字段仍被拦截。
- 生产构建及 24 项测试全部通过。

## 发布台打开本地图片文件夹

- 发布台所选素材的「图片组」标题旁添加文件夹图标，通过同源接口调用 macOS Finder 打开该组原图目录。
- 路径由素材的图片执行记录定位，不使用客户端传入路径；校验完整素材状态、目录与原图存在性及关联一致性。
- 集成测试覆盖正确定位、未完成／缺失素材和图片缺失处理，构建及 24 项测试通过。
- 浏览器实测点击图标后打开命令成功返回，显示「已在 Finder 中打开图片文件夹」。原生窗口内容未核验，系统窗口读取权限尚未授权。

## 本地配置与 Windows 快捷入口

- `config.toml` 停止 Git 跟踪并加入忽略规则，当前设备实际配置保留；新增 `config.toml.eaxmple`，首次使用需复制为本地配置。
- 启动器在配置缺失时输出明确指引；根据依赖文件、Node 主版本、系统和架构检测是否需要重新安装依赖，日常更新后双击即可准备运行环境。
- 新增 Windows 启动／停止 `.bat`；适配 npm / Codex 的 Windows 包装脚本、资源管理器和浏览器打开、监督进程树终止及带实例校验的停止请求。
- 本次在 Mac 上完成 28 项自动化测试及生产构建；新增测试覆盖 Windows 命令参数与含中文／空格路径、模板配置和保留提示词普通文字、匹配与不匹配的停止请求。
- 临时独立项目实测缺配置提示、首次安装启动、重复启动复用、停止请求与停止脚本、依赖不变复用及锁文件改变后重装。正式素材数据未用于上述测试。
- 公开远端在禁用 Git 凭据助手及交互输入时可读取；HTTPS 拉取不需要用户 Key，推送仍需写入认证。
- Windows 分支目前为代码与逻辑测试验证，尚未在 Windows 真机验收 CLI 生图、桌面窗口打开和进程树停止；不能将 Mac 验收视为 Windows 端到端通过。

## 本地提示词与仓库模板分离

- `prompts/images.md`、`prompts/copy.md` 停止 Git 跟踪并加入忽略规则；当前本机文件完整保留，修改前后 SHA-256 一致。
- `prompts/images.example.md`、`prompts/copy.example.md` 逐字复制本次修改前的实际提示词，包含用户已手工移除括号标注的版本；模板与实际文件逐字节比较一致。
- README 补充 Mac / Windows 首次复制提示词步骤、实际文件与模板的区别，以及旧版首次迁移的备份恢复说明。缺少实际提示词时，程序提示对应模板位置。
- 隔离目录验证模板复制、账号与主题替换、实际提示词修改生效；README 链接与 shell 语法检查、生产构建及 28 项自动化测试通过。
